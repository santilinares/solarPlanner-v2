# Future Considerations for Solar Planner 2.0

## Going Zoneless

- It's all about not using Zone.js for detecting when a change in the application state.
- Zone.js automatically "detects" it by scanning a lot of elements basically. SO its not efficient at all.
- Nevertheless going zoneless means treating variables differently (more code), so that's why this is a **CONSIDFERATION**.

